# =============================================================================
# Ferretería App - Environment Variables Configuration
# =============================================================================
# Copy this file to .env and adjust the values for your environment
# cp env.example .env

# -----------------------------------------------------------------------------
# Flask Configuration
# -----------------------------------------------------------------------------
SECRET_KEY=change-me-in-production-use-random-string
FLASK_DEBUG=0

# -----------------------------------------------------------------------------
# Authentication (MEJORA 8 - Password Protection)
# -----------------------------------------------------------------------------
# REQUIRED: Set a password to access the application
# This is a single password for the entire app (no users/roles)
APP_PASSWORD=change-me

# Optional: Session key for authentication (default: "authenticated")
# SESSION_AUTH_KEY=authenticated

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# OPTION 1: Use individual variables (recommended for Docker)
DB_HOST=db
DB_PORT=5432
DB_NAME=ferreteria
DB_USER=ferreteria
DB_PASSWORD=ferreteria

# OPTION 2: Use single DATABASE_URL (overrides individual variables if set)
# DATABASE_URL=postgresql://ferreteria:ferreteria@db:5432/ferreteria

# -----------------------------------------------------------------------------
# Database Configuration for EXTERNAL PostgreSQL (Modo B)
# -----------------------------------------------------------------------------
# If using external PostgreSQL (not in Docker), change DB_HOST to:
# - Windows/Mac with Docker Desktop: host.docker.internal
# - Linux: 172.17.0.1 or your host IP
# - Remote server: server IP or hostname
# 
# Example for external DB:
# DB_HOST=host.docker.internal
# DB_PORT=5432
# DB_NAME=ferreteria
# DB_USER=admin
# DB_PASSWORD=admin123

# -----------------------------------------------------------------------------
# Legacy Variables (still supported for backward compatibility)
# -----------------------------------------------------------------------------
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DB=ferreteria
# POSTGRES_USER=ferreteria
# POSTGRES_PASSWORD=ferreteria

# -----------------------------------------------------------------------------
# Notes
# -----------------------------------------------------------------------------
# - For Docker Compose (Modo A): Use DB_HOST=db
# - For External DB (Modo B): Use DB_HOST=host.docker.internal or IP
# - FLASK_DEBUG=1 for development, 0 for production
# - Change SECRET_KEY to a random string in production

